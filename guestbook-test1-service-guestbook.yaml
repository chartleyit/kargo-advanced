apiVersion: v1
kind: Service
metadata:
  name: guestbook
  namespace: guestbook-test1
spec:
  ports:
  - name: http
    nodePort: 33081
    port: 80
    targetPort: http
  selector:
    app: guestbook
  type: NodePort
