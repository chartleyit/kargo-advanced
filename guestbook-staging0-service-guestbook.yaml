apiVersion: v1
kind: Service
metadata:
  name: guestbook
  namespace: guestbook-staging0
spec:
  ports:
  - name: http
    nodePort: 32082
    port: 80
    targetPort: http
  selector:
    app: guestbook
  type: NodePort
