apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: guestbook-staging

patches:
- target:
    kind: Service
    name: guestbook
  patch: |-
    - op: add
      path: /spec/ports/0/nodePort
      value: 32081
