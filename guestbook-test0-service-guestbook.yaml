apiVersion: v1
kind: Service
metadata:
  name: guestbook
  namespace: guestbook-test0
spec:
  ports:
  - name: http
    nodePort: 32081
    port: 80
    targetPort: http
  selector:
    app: guestbook
  type: NodePort
